apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "builtin-objects-demo.fullname" . }}-test-connection"
  labels:
    {{- include "builtin-objects-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "builtin-objects-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
